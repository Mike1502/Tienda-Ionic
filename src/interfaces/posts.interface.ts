export interface Rpedido{
    error: boolean;
    mensaje: string;
}
export interface Bpedido{
    error: boolean;
    mensaje: string;
}
export interface Login{
    error: boolean;
    token: string;
    id_usuario:string;
}